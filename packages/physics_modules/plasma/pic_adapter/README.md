# Plasma — PIC Adapter (candidate)

Phase 7C skeleton adapter that composes `plasma_electromagnetic_field`
and `plasma_particle_pusher` into a single PIC step. Skeleton only;
Phase 8 brings a real Yee-grid field solver, current deposition, and
self-consistent loop.
