# Plasma — Particle Pusher (candidate)

Phase 7C interface stub for a single-particle Boris pusher. The
reference implementation lives in `src/__init__.py`; validated runs
await the Phase 8 PIC adapter that binds the pusher to a real
self-consistent field solver.
