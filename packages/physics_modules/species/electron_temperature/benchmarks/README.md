# Electron Temperature Benchmarks

Candidate module. Validation benchmark promotion is pending a reviewed
reference case; the module remains `candidate` until that evidence is
added to `module.yaml`.
